agentscope
torch
torchvision
torchaudio
transformers
datasets
accelerate
tqdm

# Note:
# - `torch` may install a CUDA-enabled wheel depending on the platform and pip resolver.
# - If you need a specific torch+cuda variant, replace `torch`, `torchvision`, `torchaudio` with
#   the exact install line from https://pytorch.org/ (for example `torch==2.2.0+cu121 -f <url>`).